# EcoShare 🌿

A Trip Expense Splitter App with a Sustainability Twist.
Seamlessly track expenses, split bills, and earn "Green Points" for eco-friendly choices.

## Features ✨
- **Expense Splitting**: Fair logic that handles new members joining mid-trip.
- **Eco-Verification**: Upload photos of sustainable actions (Bike/Train) to earn points.
    - **Proof Viewer**: See verified photos with GPS/Time metadata.
- **Premium UI**: "Emerald" theme with glassmorphism and animations.
- **Secure**: Login persistence and verification enforcement.

## 🚀 How to Run (Step-by-Step)

You will need **Node.js** installed on your laptop.

### 1. Install Dependencies
This project has two parts (Frontend and Backend). You need to install libraries for both.

**Open a terminal in the `EcoShare` folder:**
```bash
# 1. Install Frontend
npm install

# 2. Install Backend
cd server
npm install
cd ..
```

### 2. Start the App
You need two separate terminal windows running at the same time.

**Terminal 1 (Backend API):**
```bash
node server/index.js
# Output: Server running on http://localhost:3000
```

**Terminal 2 (Frontend Website):**
```bash
npm run dev
# Output: Local: http://localhost:5173
```

### 3. Open in Browser
Go to **[http://localhost:5173](http://localhost:5173)**

## 📂 Project Structure
- `src/` - React Frontend Code
- `server/` - Backend API & Database Logic
- `db.json` - The Database (Contains your users, trips, and expenses)
- `server/uploads/` - Where verification photos are stored.

---
*Generated by Antigravity*
